using System;
using System.Collections.Generic;

namespace TextAnalysis
{
    static class TextGeneratorTask
    {
        /// <summary>
        /// Возвращает следующее слово по двухсловному (bigram) или однословному ключу.
        /// Предпочитает bigram, затем unigram. Возвращает пустую строку, если продолжение не найдено.
        /// </summary>
        public static string CreateNextWord(IReadOnlyList<string> resultedPhrase, IReadOnlyDictionary<string, string> nextWords, string continuation)
        {
            if (nextWords is null) throw new ArgumentNullException(nameof(nextWords));
            if (!string.IsNullOrEmpty(continuation) && nextWords.TryGetValue(continuation, ou
