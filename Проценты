public static double Calculate(string input)
{
    var p = input.Split(' ', StringSplitOptions.RemoveEmptyEntries);
    return double.Parse(p[0]) *
           Math.Pow(1 + double.Parse(p[1]) / 1200, int.Parse(p[2]));
}
